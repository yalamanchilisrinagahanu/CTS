using System;

class Program
{
    static void Main()
    {
        int[] arr = { 10, 20, 30, 40, 50 };

        foreach (int num in arr)
        {
            Console.WriteLine(num);
        }
    }
}
